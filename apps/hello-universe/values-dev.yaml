# hello-universe (DEV) — overrides on the shared `app` chart's defaults.
# Differs from hello-world only in image + replica count.
replicaCount: 2

image:
  repository: acridpdev.azurecr.io/hello-universe
  tag: "1463e74"
